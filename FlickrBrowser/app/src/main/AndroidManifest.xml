<?xml version="1.0" encoding="utf-8"?>
<manifest package="net.android.jason.flickrbrowser"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Flickr">
        <activity
            android:name="net.android.jason.flickrbrowser.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name="net.android.jason.flickrbrowser.SearchActivity"
            android:label="@string/title_activity_search"
            android:parentActivityName="net.android.jason.flickrbrowser.MainActivity"
            android:theme="@style/Theme.Flickr">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="net.android.jason.flickrbrowser.MainActivity"/>

            <intent-filter>
                <action android:name="android.intent.action.SEARCH"/>

                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable"/>
        </activity>
        <activity
            android:name="net.android.jason.flickrbrowser.ViewPhotoDetailsActivity"
            android:label="@string/title_activity_view_photo_details"
            android:parentActivityName="net.android.jason.flickrbrowser.MainActivity"
            android:theme="@style/Theme.Flickr">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="net.android.jason.flickrbrowser.MainActivity"/>
        </activity>
    </application>

</manifest>
